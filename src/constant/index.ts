import { createArray } from "@/utility";

export const PASS_CODE = "270620";

export const PREFIX_CLOUD_IMAGE = "v1703143426/menu4linh";

export const TIME = {
  HOURS: createArray(24),
  MINUTES: createArray(60),
};
